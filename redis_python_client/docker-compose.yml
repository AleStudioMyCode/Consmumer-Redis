version: "3.8"
services:
  redis:
    image: redis:7
    container_name: redis_local
    ports:
      - "6379:6379"

  api:
    build: .
    container_name: redis_api
    ports:
      - "8000:8000"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      - redis
